class Groups {
  constructor(group_ID, group_heads, group_name, group_icon) {
    this.group_ID = group_ID;
    this.group_heads = group_heads;
    this.group_name = group_name;
    this.group_icon = group_icon;
  }
}

export default Groups;
