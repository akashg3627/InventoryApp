class Groups {
  constructor(group_ID, group_heads, group_name, group_icon) {
    this.group_ID = group_ID;
    this.group_heads = group_heads; //updatable
    this.group_name = group_name; //updatable
    this.group_icon = group_icon; //updatable
  }
}

export default Groups;
